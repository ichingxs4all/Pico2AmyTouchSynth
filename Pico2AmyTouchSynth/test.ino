// #include <AMY-Arduino.h>

// void test(){
//  amy_event e = amy_default_event();

//             e.echo_delay_ms = 1;
//             e.echo_feedback = 1;
//             e.echo_max_delay_ms = 1;
//             e.echo_level = 1;
//             e.echo_filter_coef = 1;
            
            

//             e.resonance = 0.7 ; //0.5-16.0. default 0.7

          
//             e.volume = 1.0;
            

//             e.eq_h = 0; // Equalization in dB low (~800Hz) / med (~2500Hz) / high (~7500Hz) -15 to 15. 0 is off. default 0.
//             e.eq_m = 0;
//             e.eq_l = 0;

//             e.freq_coefs = 1;


          
//             amy_add_event(&e);

// }